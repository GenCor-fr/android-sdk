apply plugin: 'maven-publish'

publishing {
    publications {
        release(MavenPublication) {
            groupId "tech.kissmyapps"
            artifactId "android-sdk"
            version "1.0.0"

            afterEvaluate {
                from components.release
            }
        }
    }

    repositories {
        maven {
            url "https://git.netpeak.net/api/v4/projects/6017/packages/maven"
            credentials(HttpHeaderCredentials) {
                name = "Private-Token"
                value = "glpat-Uv_V8VcuuHmzXyyFNnzX"
            }
            authentication {
                header(HttpHeaderAuthentication)
            }
        }
    }
}
